- hosts: all

  tasks:
    - name: install git
      ansible.builtin.apt:
        name:
          - git
          - git-core
        state: present
        update_cache: yes
      become: yes